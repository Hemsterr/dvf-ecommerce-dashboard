// Libs
import React from 'react'
import { storiesOf } from '@storybook/react'

// Components
import FittingItem from './index'

storiesOf('FittingItem', module).add('Default', () => (
  <FittingItem title="I would like to fit myself using Hemster's Fitting Kit" />
))
