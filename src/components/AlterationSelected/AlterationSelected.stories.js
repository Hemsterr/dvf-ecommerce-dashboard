// Libs
import React from 'react'
import { storiesOf } from '@storybook/react'

// Components
import AlterationItem from './index'

storiesOf('AlterationItem', module).add('Default', () => (
  <AlterationItem alteration="Ciara Satin Crepe Pants" />
))
